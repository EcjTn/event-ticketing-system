import type IEvent from "../types/IEvent"

interface EventCardProps {
    event: IEvent
}

function EventCard({ event }: EventCardProps) {
    return (
        <div>
            {/* TODO: implement full event card */}
        </div>
    )
}

export default EventCard